{
  "name": "reverser",
  "packageManager": "pnpm@8.8.0",
  "private": true,
  "scripts": {
    "build": "esbuild app/typescript/application.ts --target=chrome111,firefox111,safari16 --bundle --sourcemap --outdir=public/build --loader:.png=file"
  },
  "version": "0.1.0",
  "devDependencies": {
    "esbuild": "^0.19.4"
  }
}
