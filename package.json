{
  "name": "reverser",
  "packageManager": "yarn@3.1.1",
  "private": true,
  "dependencies": {
    "@rails/ujs": "^7.0.0",
    "esbuild": "^0.14.3",
    "sass": "^1.44.0"
  },
  "scripts": {
    "build": "esbuild app/typescript/*.* --bundle --sourcemap --outdir=./public/build",
    "build:css": "sass ./app/stylesheet/application.sass.scss ./public/build/application.css --load-path=node_modules --embed-sources",
    "tsc": "tsc --noEmit"
  },
  "version": "0.1.0",
  "devDependencies": {
    "@types/rails__ujs": "^6.0.1",
    "typescript": "^4.5.4"
  }
}
