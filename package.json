{
  "name": "reverser",
  "packageManager": "yarn@3.2.4",
  "private": true,
  "dependencies": {
    "@rails/ujs": "^7.0.4"
  },
  "scripts": {
    "build": "esbuild app/typescript/*.* --bundle --sourcemap --outdir=./public/build",
    "build:css": "sass ./app/stylesheet/application.scss ./public/build/application.css --load-path=node_modules --embed-sources",
    "tsc": "tsc --noEmit"
  },
  "version": "0.1.0",
  "devDependencies": {
    "@types/rails__ujs": "^6.0.1",
    "esbuild": "^0.15.12",
    "sass": "^1.55.0",
    "typescript": "^4.8.4"
  }
}
