<footer>
  Running <%= "#{Config.app_name}" %>
  &ndash; <%= link_to "About", about_static_path %>
  &ndash; <%= link_to "Contact", contact_static_path %>
  <% if GitHelper.enabled? %>
    &ndash; <%= link_to "Github", GitHelper.url %> (<%= link_to GitHelper.hash, "#{GitHelper.url}/tree/#{GitHelper.hash}" %>)
  <% end %>
</footer>
