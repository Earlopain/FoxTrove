<footer>
  <%= "#{Config.app_name}" %>
  <% if GitHelper.enabled? %>
    – <%= link_to "Github", GitHelper.url %> (<%= link_to GitHelper.hash, "#{GitHelper.url}/tree/#{GitHelper.hash}" %>)
  <% end %>
</footer>
