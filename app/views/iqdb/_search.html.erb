<%# locals: () -%>

<%= form_with scope: :search, url: search_iqdb_path do |f| %>
  <%= f.input :url, label: "URL", placeholder: "Also supports e621 post links" %>
  <%= f.input :file, as: :file, accept: "image/*,video/*" %>

  <%= f.submit "Search" %>
<% end %>
