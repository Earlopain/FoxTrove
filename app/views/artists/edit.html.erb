<%= render "secondary_links" %>

Current Links:
<div class="url-list">
  <% @artist.artist_urls.each do |artist_url| %>
    <%= render "artist_link", artist_url: artist_url %>
  <% end %>
</div>
<%= @artist.errors.full_messages.join(", ") %>
<%= simple_form_for @artist do |f| %>
  <%= f.input :name, as: :string %>
  <%= f.input :url_string, as: :text, label: "New Links", input_html: { rows: 10, cols: 50 } %>
  <%= f.button :submit %>
<% end %>

<% page_title "Edit #{@artist.name}" %>
