Current Links:
<%= render "artist_urls/list", artist_urls: ordered_artist_urls(@artist) %>
<%= form_errors(@artist) %>
<%= form_with model: @artist do |f| %>
  <%= f.input :name, as: :string %>
  <%= f.input :url_string, as: :textarea, label: "New Links", rows: 10, cols: 50 %>
  <%= f.submit %>
<% end %>

<% page_title "Edit #{@artist.name}" %>
