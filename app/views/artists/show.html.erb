<%= render "secondary_links" %>

Name: <%= @artist.name %>
<br>

Links:
<div class="url-list">
  <% @artist.artist_urls.each do |url| %>
    <%= render "artist_link", url: url %>
  <% end %>
</div>

<% @recent_submissions.each do |submission| %>
  <%= render "submission/files", submission: submission %>
<% end %>
